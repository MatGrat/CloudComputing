apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (9532ceef3)
  labels:
    io.kompose.service: cloudcomputing-hs-frontend
  name: cloudcomputing-hs-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: cloudcomputing-hs-frontend
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.35.0 (9532ceef3)
      labels:
        io.kompose.service: cloudcomputing-hs-frontend
    spec:
      containers:
        - args:
            - npm
            - start
          image: cloudcomputing-hs-frontend
          imagePullPolicy: Never
          name: cloudcomputing-hs-frontend
          ports:
            - containerPort: 3000
              protocol: TCP
          volumeMounts:
            - mountPath: /app/public
              name: cloudcomputing-hs-frontend-cm0
            - mountPath: /app/src
              name: cloudcomputing-hs-frontend-cm1
      restartPolicy: Always
      volumes:
        - configMap:
            name: cloudcomputing-hs-frontend-cm0
          name: cloudcomputing-hs-frontend-cm0
        - configMap:
            name: cloudcomputing-hs-frontend-cm1
          name: cloudcomputing-hs-frontend-cm1
